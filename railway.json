{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "packages/server/Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '>>> running migrations' && npx prisma migrate deploy --schema prisma/schema.prisma && echo '>>> migrations done, starting node' && node dist/index.js; echo \">>> process exited with code $?\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
