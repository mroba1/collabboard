{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run build:shared && npm run build --workspace=packages/client",
  "outputDirectory": "packages/client/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
